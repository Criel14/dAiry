interface:
  display_name: "dAiry 日记代写"
  short_description: "远程整理正文并写入 dAiry Markdown 日记"
  default_prompt: "使用 $dairy-journal-writer 帮我把今天的日记整理并写入 dAiry。"

policy:
  allow_implicit_invocation: true
